# Leave empty to use same-origin requests (the Vite dev proxy in development,
# nginx in the Docker image). Set it to an absolute URL when the API is hosted
# on a different domain, e.g. a separate AWS App Runner service.
VITE_API_BASE_URL=
